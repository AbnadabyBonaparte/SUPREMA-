/**
 * @deprecated Este arquivo está obsoleto. Use '@/lib/supabaseClient' em vez disso.
 * Mantido apenas para compatibilidade com imports antigos.
 * 
 * MIGRAÇÃO: Substitua todos os imports deste arquivo por:
 * import { supabase, saveLead } from '@/lib/supabaseClient';
 */

// Re-export do cliente centralizado
export { supabase, saveLead } from '@/lib/supabaseClient';
